spring:
  datasource:
    url: jdbc:h2:mem:fitmarmita-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

  flyway:
    enabled: false

fitmarmita:
  jwt:
    secret: chave-de-teste-com-tamanho-suficiente-para-hmac-sha256-funcionar
    expiration-hours: 1
